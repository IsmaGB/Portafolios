/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export', 
  basePath: '/Portafolios', 
  assetPrefix: '/Portafolios/', 
};

module.exports = nextConfig;
